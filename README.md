# news
Webapp to consolidate federated free news articles


## Running

To run this application, navigate to the project directory (news). Then run the following commands:

1. `export PYTHONPATH=src`
2. `fastapi dev src/main.py`

Note: you only need to set the path once. As long as you maintain the same terminal, you will only need to run the second command.